# File này chỉ liệt kê tổng quan. File .env thật cần tạo riêng ở:
#   backend/.env   (copy từ backend/.env.example)
#   frontend/.env  (copy từ frontend/.env.example)

# ----- backend -----
# PORT=5000
# DATABASE_URL="postgresql://username:password@localhost:5432/chinese_vocabulary"
# JWT_SECRET="change-me"
# JWT_EXPIRES_IN="7d"

# ----- frontend -----
# VITE_API_URL=http://localhost:5000/api
