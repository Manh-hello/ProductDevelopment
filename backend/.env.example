PORT=5000
DATABASE_URL="postgresql://username:password@localhost:5432/chinese_vocabulary"

# JWT chưa được sử dụng ở Phase 2, nhưng khai báo sẵn để Phase 3
# (Authentication) chỉ cần điền giá trị thật, không cần sửa code đọc env.
JWT_SECRET="change-me"
JWT_EXPIRES_IN="7d"
